services:
  # Node.js Express application
  pdf-server:
    build: ./pdf-server
    container_name: pdf-server
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - app-network

networks:
  app-network:
    driver: bridge